ОПРЕДЕЛИТЬ КЛАСС Настройки (
    ОПРЕДЕЛИТЬ КОНСТРУКТОР (ссылка) () (
        ссылка:сырые_данные = парсер();

        ссылка:токен = извлечь_из_таблицы(ссылка:сырые_данные, "ТОКЕН");
        ссылка:адрес = извлечь_из_таблицы(ссылка:сырые_данные, "АДРЕС");
        ссылка:кол_во_событий = в_число(извлечь_из_таблицы(ссылка:сырые_данные, "КОЛ_ВО_СОБЫТИЙ"));
    )
)
