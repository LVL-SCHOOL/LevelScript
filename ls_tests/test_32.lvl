ВКЛЮЧИТЬ стандартная_библиотека.*


ОПРЕДЕЛИТЬ ПРОЦЕДУРУ главная() (
    ЗАДАТЬ массив_слов = массив("Привет", "это", "мой", "язык", "программирования", "!!!");

    ЦИКЛ индекс ОТ 0 ДО длина_массива(массив_слов) - 1 (
        печать(массив(верхний_регистр(достать_из_массива(массив_слов, индекс))), "", "");
        !спать(0.1);
    )

    НАПЕЧАТАТЬ получить_тип(массив());
    НАПЕЧАТАТЬ получить_тип(таблица());
    НАПЕЧАТАТЬ получить_тип(Список);
    НАПЕЧАТАТЬ получить_тип(Список());
    НАПЕЧАТАТЬ получить_тип("");
    НАПЕЧАТАТЬ получить_тип(1);
    НАПЕЧАТАТЬ получить_тип(БазоваяОшибка);
    НАПЕЧАТАТЬ получить_тип(массив());
    НАПЕЧАТАТЬ получить_тип(таблица());
    НАПЕЧАТАТЬ получить_тип(Список);
    НАПЕЧАТАТЬ получить_тип(Список());
    НАПЕЧАТАТЬ получить_тип("");
    НАПЕЧАТАТЬ получить_тип(1);
    НАПЕЧАТАТЬ получить_тип(БазоваяОшибка());
    НАПЕЧАТАТЬ получить_тип(ИСТИНА);
    печать(массив(), "1", "2");

    НАПЕЧАТАТЬ получить_тип(получить_тип(1));
    НАПЕЧАТАТЬ получить_тип(получить_тип(получить_тип(1)));
    НАПЕЧАТАТЬ получить_тип(массив(получить_тип));
    НАПЕЧАТАТЬ массив(получить_тип);
)

ВЫПОЛНИТЬ (
    главная();
)
