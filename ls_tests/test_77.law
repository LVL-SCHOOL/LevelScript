ВКЛЮЧИТЬ стандартная_библиотека.*
ВЫПОЛНИТЬ (
    вывод("прив"); вывод(
        "прив",
         !1 / 1,
    ); вывод("прив2");вывод("прив3"); вывод(
    "прив4",
    );
    вывод(!вывод(((
    массив(1, 2,3));
    !вывод(в_строку(
    !массив(3, 2,1)));

)
