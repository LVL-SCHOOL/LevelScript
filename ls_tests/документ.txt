ВКЛЮЧИТЬ закон

СОЗДАТЬ ФАКТИЧЕСКУЮ СИТУАЦИЮ название_фактической_ситуации2 (
    ОБЪЕКТ название_объекта,
    СУБЪЕКТ название_субъекта,

   ДАННЫЕ (
		ТЕСТОВОЕ_ЗНАЧЕНИЕ 100,
		ПРОЦЕДУРНАЯ_ПРОВЕРКА 100,
		#ЕДИНИЦА мг/л,
        ДОПУСТИМОЕ_ЗНАЧЕНИЕ 11,
        #ТИП Сброс через трубопровод,
        МИНИМАЛЬНОЕ_ЗНАЧЕНИЕ 0.1,
        СРЕДНЕЕ_ЗНАЧЕНИЕ 1.6,
        ТЕСТОВОЕ_ЗНАЧЕНИЕ2 40,
    )
)

ПРОВЕРКА проверка_субъекта (
	ФАКТИЧЕСКАЯ СИТУАЦИЯ название_фактической_ситуации2,
	ДОКУМЕНТ название_документа,
)

ПРОВЕРКА проверка_субъекта3 (
	ФАКТИЧЕСКАЯ СИТУАЦИЯ название_фактической_ситуации2,
	ДОКУМЕНТ название_документа_2,
)


ОПРЕДЕЛИТЬ ПРОЦЕДУРУ название_процедуры223214324(     аргумент2)(
    ЗАДАТЬ переменная = (2+2)*2-аргумент2  ;
    ЗАДАТЬ массив = [1 ,    2,4  ]  ;
    ЕСЛИ аргумент БОЛЬШЕ 10 TO (
        ЕСЛИ аргумент БОЛЬШЕ 10 TO (
            ЗАДАТЬ переменная = (2+2)*2-аргумент2;
        )
    )

    ЕСЛИ аргумент БОЛЬШЕ 10 TO (
        ЦИКЛ ОТ (2+2)*2-аргумент1 ДО (2+2)*2-аргумент2 (
            ЕСЛИ аргумент БОЛЬШЕ 10 TO (
                ЦИКЛ ОТ (2+2)*2-аргумент1 ДО (2+2)*2-аргумент2 (
                    НАПЕЧАТАТЬ массив[1];
                )
            )

        )
    )
    ИНАЧЕ (
    ЕСЛИ аргумент БОЛЬШЕ 10 TO (
        ЦИКЛ ОТ (2+2)*2-аргумент1 ДО (2+2)*2-аргумент2 (
            ЕСЛИ аргумент БОЛЬШЕ 10 TO (
        ЦИКЛ ОТ (2+2)*2-аргумент1 ДО (2+2)*2-аргумент2 (
            ЕСЛИ аргумент БОЛЬШЕ 10 TO (
                ЦИКЛ ОТ (2+2)*2-аргумент1 ДО (2+2)*2-аргумент2 (
                    НАПЕЧАТАТЬ массив[1];
                )
            )
    ЕСЛИ аргумент БОЛЬШЕ 10 TO (
        ЦИКЛ ОТ (2+2)*2-аргумент1 ДО (2+2)*2-аргумент2 (
            ЕСЛИ аргумент БОЛЬШЕ 10 TO (
                ЦИКЛ ОТ (2+2)*2-аргумент1 ДО (2+2)*2-аргумент2 (
                    НАПЕЧАТАТЬ массив[1];
                )
            )
    ЕСЛИ аргумент БОЛЬШЕ 10 TO (
        ЦИКЛ ОТ (2+2)*2-аргумент1 ДО (2+2)*2-аргумент2 (
            ЕСЛИ аргумент БОЛЬШЕ 10 TO (
                ЦИКЛ ОТ (2+2)*2-аргумент1 ДО (2+2)*2-аргумент2 (
                    ЕСЛИ аргумент БОЛЬШЕ 10 TO (
        ЦИКЛ ОТ (2+2)*2-аргумент1 ДО (2+2)*2-аргумент2 (
            ЕСЛИ аргумент БОЛЬШЕ 10 TO (
                ЦИКЛ ОТ (2+2)*2-аргумент1 ДО (2+2)*2-аргумент2 (
                    НАПЕЧАТАТЬ массив[1];
                )
                 ЦИКЛ ОТ (2+2)*2-аргумент1 ДО (2+2)*2-аргумент2 (
                    НАПЕЧАТАТЬ массив[1];
                )
                 ЦИКЛ ОТ (2+2)*2-аргумент1 ДО (2+2)*2-аргумент2 (
                    НАПЕЧАТАТЬ массив[1];
                )
                 ЦИКЛ ОТ (2+2)*2-аргумент1 ДО (2+2)*2-аргумент2 (
                    НАПЕЧАТАТЬ массив[1];
                )
                 ЦИКЛ ОТ (2+2)*2-аргумент1 ДО (2+2)*2-аргумент2 (
                    НАПЕЧАТАТЬ массив[1];
                )
            )

        )
    )

                    НАПЕЧАТАТЬ массив[1];
                )
            )

        )
    )
        )
    )
        )
    )

            ЕСЛИ аргумент БОЛЬШЕ 10 TO (
                ЦИКЛ ОТ (2+2)*2-аргумент1 ДО (2+2)*2-аргумент2 (
                    НАПЕЧАТАТЬ массив[1];
                )
            )

        )
    )
    )

    ЗАДАТЬ дата = ДАТА_СЕГОДНЯ();

    ЕСЛИ аргумент БОЛЬШЕ 10 И масив[1] РАВНО 1 И 2 + 2 РАВНО 1  TO (

    )
    ИНАЧЕ (
        # ВЕРНУТЬ строка;
    )

    ЗАДАТЬ строка = это строка, а ты что думал?!);

    ВЕРНУТЬ строка;


)



ОПРЕДЕЛИТЬ ПРОЦЕДУРУ dffdggfd(     аргумент2)(
    ЗАДАТЬ переменная = (2+2)*2-аргумент2  ;
    ЗАДАТЬ массив = [1 ,    2,4  ]  ;
    ЕСЛИ аргумент БОЛЬШЕ 10 TO (
        ЕСЛИ аргумент БОЛЬШЕ 10 TO (
            ЗАДАТЬ переменная = (2+2)*2-аргумент2;
        )
    )

    ЕСЛИ аргумент БОЛЬШЕ 10 TO (
        ЦИКЛ ОТ (2+2)*2-аргумент1 ДО (2+2)*2-аргумент2 (
            ЕСЛИ аргумент БОЛЬШЕ 10 TO (
                ЦИКЛ ОТ (2+2)*2-аргумент1 ДО (2+2)*2-аргумент2 (
                    НАПЕЧАТАТЬ массив[1];
                )
            )

        )
    )
    ИНАЧЕ (

    ЕСЛИ аргумент БОЛЬШЕ 10 TO (
        ЦИКЛ ОТ (2+2)*2-аргумент1 ДО (2+2)*2-аргумент2 (
            ЕСЛИ аргумент БОЛЬШЕ 10 TO (
                ЦИКЛ ОТ (2+2)*2-аргумент1 ДО (2+2)*2-аргумент2 (
                    НАПЕЧАТАТЬ массив[1];
                )
            )

        )
    )
    ИНАЧЕ (

    ЕСЛИ аргумент БОЛЬШЕ 10 TO (
        ЦИКЛ ОТ (2+2)*2-аргумент1 ДО (2+2)*2-аргумент2 (
            ЕСЛИ аргумент БОЛЬШЕ 10 TO (
                ЦИКЛ ОТ (2+2)*2-аргумент1 ДО (2+2)*2-аргумент2 (
                    НАПЕЧАТАТЬ массив[1];
                )
            )

        )
    )
    ИНАЧЕ (

    ЕСЛИ аргумент БОЛЬШЕ 10 TO (
        ЦИКЛ ОТ (2+2)*2-аргумент1 ДО (2+2)*2-аргумент2 (
            ЕСЛИ аргумент БОЛЬШЕ 10 TO (
                ЦИКЛ ОТ (2+2)*2-аргумент1 ДО (2+2)*2-аргумент2 (
                    НАПЕЧАТАТЬ массив[1];
                )
            )

        )
    )
    ИНАЧЕ (

    ЕСЛИ аргумент БОЛЬШЕ 10 TO (
        ЦИКЛ ОТ (2+2)*2-аргумент1 ДО (2+2)*2-аргумент2 (
            ЕСЛИ аргумент БОЛЬШЕ 10 TO (
                ЦИКЛ ОТ (2+2)*2-аргумент1 ДО (2+2)*2-аргумент2 (
                    НАПЕЧАТАТЬ массив[1];
                    ВЕРНУТЬ 1;
                )
            )


        )
    )
    ИНАЧЕ (
    ВЕРНУТЬ 1;
    )
    )
    )
    )

    )
    ВЕРНУТЬ 1;
)
