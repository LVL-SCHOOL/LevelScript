ВКЛЮЧИТЬ стандартная_библиотека.структуры.*;ВКЛЮЧИТЬ стандартная_библиотека.типы;
ВКЛЮЧИТЬ стандартная_библиотека.*; !ВКЛЮЧИТЬ asdasd.*;


ОПРЕДЕЛИТЬ ПРОЦЕДУРУ тест(a...) (
    ЗАДАТЬ с = точная_временная_метка();
-((((((1))))))
    НАПЕЧАТАТЬ (((
    получить_тип(
    массив(
    )
    )

)))
    ЦИКЛ ОТ 1 ДО 100000 (
        2 + 2 * 2
    )
    НАПЕЧАТАТЬ (
    массив(-1,1,
    )
    ); -(-1)
    вывод(точная_временная_метка() -с )
)

ВЫПОЛНИТЬ (
    тест((((((((((((массив(массив(массив(массив(массив(
    ),
    ))))))))))))))));    тест(массив(массив(массив(массив(массив(
    )
    )))))
)
