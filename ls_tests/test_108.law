ВКЛЮЧИТЬ стандартная_библиотека.конкурентность
ВКЛЮЧИТЬ стандартная_библиотека.интернет
ВКЛЮЧИТЬ стандартная_библиотека.строки
ВКЛЮЧИТЬ стандартная_библиотека.структуры.сложные_структуры


ОПРЕДЕЛИТЬ ПРОЦЕДУРУ фоновая_задача(номер) (

    НАПЕЧАТАТЬ форматировать_строку("Задача номер {}, делает свои дела...", номер);
    запрос_в_интернет("GET", "https://ya.ru", таблица());
    НАПЕЧАТАТЬ форматировать_строку("Задача номер {}, выполнена!", номер);

)

ОПРЕДЕЛИТЬ ПРОЦЕДУРУ главная() (
    ЗАДАТЬ задачи = Список();

    ЦИКЛ номер ОТ 1 ДО 5 (
        задачи:добавить(В ФОНЕ запрос_в_интернет("GET", "https://ya.ru", таблица()));
    )

    ждать_всех(задачи:в_массив());
)

ВЫПОЛНИТЬ (
    главная();
)
