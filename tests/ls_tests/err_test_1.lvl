ВКЛЮЧИТЬ стандартная_библиотека.интернет

ОПРЕДЕЛИТЬ ПРОЦЕДУРУ запуск_программы() (
    !ЗАДАТЬ результат = запрос_в_интернет(запрос_в_интернет("GET" "https://ya.ru", таблица()), "https://ya.ru", таблица());
    1 + 1
   !тест(а:б 1);
   !тест(1 а:б);
   !тест(а:в а:б);
    ! тест(ЖДАТЬ тест(), а:б);
   !тест(арг,);
   !тест(арг 1);
   !ЦИКЛ ОТ ДО (
   !ЦИКЛ ОТ 1 ДО (
   !ЦИКЛ ОТ (
   !ЦИКЛ ОТ
  !ЕСЛИ (



   !ЦИКЛ счет ОТ 2 + 2 * 2 - ЖДАТЬ В ФОНЕ тест() ДО запрос_в_интернет(запрос_в_интернет("GET" "https://ya.ru", таблица()), "https://ya.ru", таблица()) (
   !)



   !ЦИКЛ счет ОТ ДО запрос_в_интернет(запрос_в_интернет("GET" "https://ya.ru", таблица()), "https://ya.ru", таблица())
   !)

   ! ПОКА (

   !ЕСЛИ ТО (

   !ИНАЧЕ ЕСЛИ

    !ИНАЧЕ ЕСЛИ s ТО(
    !)

    !ОБРАБОТЧИК (

    !ОБРАБОТЧИК ав КАК фы (

)

ВЫПОЛНИТЬ (
    запуск_программы();
)
